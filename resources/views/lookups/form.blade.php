@extends('layouts.app')
@section('content')
<section class="page-head"><div><p class="eyebrow">Settings / {{ $type === 'source' ? 'Lead source' : 'Interested service' }}</p><h1>{{ $item->exists ? 'Edit' : 'Create' }} {{ $type === 'source' ? 'lead source' : 'interested service' }}</h1><p class="subhead">This option will appear in the lead form and API responses.</p></div></section>
<form class="form-panel" method="POST" action="{{ $item->exists ? route('settings.lookup.update', [$type, $item->id]) : route('settings.lookup.store', $type) }}">@csrf @if($item->exists) @method('PUT') @endif<div class="form-section"><div><p class="eyebrow">Option details</p><h2>{{ $type === 'source' ? 'Source' : 'Service' }}</h2></div><div class="form-grid"><label class="span-2">Name *<input name="name" value="{{ old('name', $item->name) }}" required autofocus></label>@if($type === 'source')<label>Color<input type="color" name="color" value="{{ old('color', $item->color ?: '#4DA3A7') }}"></label>@endif</div></div><div class="form-actions"><a class="text-link" href="{{ route($type === 'source' ? 'settings.sources' : 'settings.services') }}">Cancel</a><button class="button button-primary" type="submit">{{ $item->exists ? 'Save changes' : 'Create option' }} <span>↗</span></button></div></form>
@endsection
